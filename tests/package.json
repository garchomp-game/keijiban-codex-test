{
  "name": "tests",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "e2e": "playwright test",
    "lint": "echo 'No linting configured'",
    "typecheck": "echo 'No type checking configured'"
  }
}
